# Phase 2B Contract (Executor)

## State check
- Read `.kyros_state.json`: `current_phase=phase_2b`, `last_test_status=pass`.
- Ran `uv run pytest`: **578 passed** (Phases 1/2/3A/3B green).

## Frozen boundaries (must not violate)
- `workspace/detectors/`: only permitted change is **adding** `fibonacci.py` and its single export line in `detectors/__init__.py`. No edits to other detectors.
- `workspace/trading/`: only additive changes per Phase 2B layout; `_nearest_dol` must remain byte-identical; existing defaults byte-identical.
- `workspace/backtesting/` and `workspace/tuning/`: off-limits.

## What exists already (observed)
The Phase 2B blueprint has already been implemented in this workspace:
- `workspace/detectors/fibonacci.py` exists and is imported by `snapshot.py`.
- `workspace/trading/config.py` already contains fib_* knobs, `irl_sources`, `dol_use_cycle`, and `("fib_levels", 1)` recency cap.
- `workspace/trading/snapshot.py` already contains:
  - `LiquidityPool.scope`/`role` and `to_dict()` emitting them.
  - `MarketSnapshot.fib_levels`, `liquidity_cycle`, `dol_target`.
  - `_build_pools` including ERL swing pools and IRL sources (FVG midpoint, OB centre, equilibrium, OTE) plus scope refinement.
  - `_derive_liquidity_cycle` and a cycle-aware `_dol_target` (with fallback to `_nearest_dol`).
- `workspace/trading/reasoning_agent.py` prompt already references `liquidity_cycle`, `dol_target`, and `fib_levels`.

## Delta required in this round
No implementation changes are required because:
- All tests are green.
- The workspace already contains the Phase 2B components.

If a `review.md` is provided with BLOCK/WARNING findings, changes will be limited strictly to the allowed carve-outs and mapped here by file:line.
