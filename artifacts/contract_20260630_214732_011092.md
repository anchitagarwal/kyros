# Phase 2B Contract (Executor)

## State / Preconditions
- Checked `.kyros_state.json`: `current_phase=phase_2b`, branch `phase-2b-fib-liquidity-cycle`.
- Ran `uv run pytest`: **561 passed** (Phases 1/2/3A/3B green).

## Frozen boundaries (enforced)
- `workspace/detectors/`: only permitted change is **adding** `fibonacci.py` and its single export line in `detectors/__init__.py`. No other detector edits.
- `workspace/trading/`: additive-only changes per Phase 2B layout. `_nearest_dol` must remain byte-identical.
- `workspace/backtesting/` and `workspace/tuning/`: off-limits.

## What exists already (as of this contract)
The Phase 2B blueprint has already been implemented in the workspace:
- `workspace/detectors/fibonacci.py` exists.
- `workspace/trading/config.py` includes fib knobs, IRL sources, cycle toggle, ranked DOL knobs, and weight maps.
- `workspace/trading/snapshot.py` includes:
  - `LiquidityPool.scope/role/clarity_score/score_breakdown` and serializer fields.
  - `MarketSnapshot.fib_levels/liquidity_cycle/ranked_dols/dol_target`.
  - `_build_pools` with ERL+IRL sources, scope refinement, and ERL dedup.
  - `_derive_liquidity_cycle`, `_score_pool`, `_rank_dols`, and cycle-aware `_dol_target` with ERL-only `_nearest_dol` fallback.
- Tests already include Phase 2B additions (`tests/phase2/test_fib_liquidity_cycle.py`, `tests/test_fibonacci.py`) and are passing.

## Changes planned in this phase
**None.** Since the implementation and tests are already present and green, no code changes will be made to avoid violating frozen boundaries.

## Mapping to blueprint / review repairs
Already satisfied in current codebase:
- Repair #1: ERL-only `_nearest_dol` fallback when cycle inactive.
- Repair #2: direction-side filter in `_rank_dols`.
- Repair #3: OTE zone derived from explicit ratios (covered by `tests/test_fibonacci.py`).
- Repair #4: scope derived once via HTF fib range refinement.
- Repair #5: dedup coincident ERL sources on `(level,type)` with richest role.
- Repair #6: clean_path penalty counts opposing unswept external pools.
- Repair #7: reasoning prompt updated (covered by `tests/phase2/test_reasoning_agent.py`).
- Repair #8: precomputed swings threaded into `detect_fibonacci` and snapshot build.
